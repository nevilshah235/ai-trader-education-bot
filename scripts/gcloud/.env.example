# Use apps/backend/.env for both local backend and gcloud deployment.
# Copy apps/backend/.env.example to apps/backend/.env and add GCP_PROJECT, GCP_REGION, GEMINI_API_KEY (and optional gcloud vars).
# The scripts in this directory automatically source apps/backend/.env when present.
