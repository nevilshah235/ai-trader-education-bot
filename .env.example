# Copy to .env and set values. See apps/frontend rsbuild config for injected vars.

# Deriv third-party OAuth (optional). When set, use your own app at api.deriv.com for login.
# Register at https://api.deriv.com, set OAuth Redirect URL to your app origin (e.g. tunnel or Vercel URL), then set:
# DERIV_APP_ID=
# DERIV_OAUTH_REDIRECT_URI=

# Backend / Agent Analysis (see also apps/backend/.env.example)
# Backend
PORT=3000

# Database: omit for SQLite (default). For local Postgres (e.g. docker compose up -d postgres):
# DATABASE_URL=postgresql+psycopg2://postgres:postgres@127.0.0.1:5432/trader_edu

# Agent Analysis (Analyst/Tutor)
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Agent Analysis API URL (default: http://localhost:8000; use '' when dev proxy is active)
# AGENT_ANALYSIS_API_URL=http://localhost:8000
