# Frontend env (see rsbuild.config.ts source.define for injected vars)
# EDUCATION_API_URL=https://your-backend.run.app
# AGENT_ANALYSIS_API_URL=  # Agent Analysis API. Unset/empty = use same-origin /api (proxied to backend). Set for direct URL (e.g. http://localhost:8000).
# TRANSLATIONS_CDN_URL=...
# GD_CLIENT_ID=...
# GD_APP_ID=...
# GD_API_KEY=...

# Deriv third-party OAuth (optional). When set, use your own app at api.deriv.com for login.
# Register at https://api.deriv.com — set "Website URL" / OAuth Redirect URL to your app origin (e.g. https://your-ngrok-url.ngrok-free.app).
# DERIV_APP_ID=
# Optional: force redirect_uri in authorize URL (only if Deriv rejects; usually leave unset so Deriv uses the registered URL).
# DERIV_OAUTH_REDIRECT_URI=
