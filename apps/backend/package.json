{
  "name": "backend",
  "private": true,
  "version": "0.0.1",
  "engines": {
    "node": "20.x"
  },
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "dev:agent": "uv run --project . sh -c 'cd src && uvicorn main:app --reload --port 8000 --http h11'"
  },
  "dependencies": {
    "@ai-edu/shared": "0.0.1",
    "@hono/node-server": "^1.13.0",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.3"
  }
}
