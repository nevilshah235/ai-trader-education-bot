# Backend env
PORT=3000

# Logging: DEBUG, INFO, WARNING, ERROR (default INFO). Use DEBUG to trace DB/agent errors.
# LOG_LEVEL=DEBUG

# Database: omit for SQLite (defaults to apps/backend/src/data/transactions.db)
# DATABASE_URL=sqlite:///./data/transactions.db
# Google Cloud PostgreSQL (Cloud SQL): use Secret Manager for password in production
# DATABASE_URL=postgresql+psycopg2://user:password@/dbname?host=/cloudsql/PROJECT:REGION:INSTANCE
# Local dev with Cloud SQL Proxy: DATABASE_URL=postgresql+psycopg2://user:pass@127.0.0.1:5432/dbname

# Agent Analysis (Gemini)
# GEMINI_API_KEY=

# RAG / Search (NVIDIA API for embeddings + LLM)
# BINDING=nvidia
# BINDING_HOST=https://integrate.api.nvidia.com/v1
# API_KEY=
# EMBEDDING_MODEL=baai/bge-m3
# LLM_MODEL=meta/llama-3.3-70b-instruct
# INDEX_DIR=search_index
# DATA_DIR=data

# ---- GCloud deployment (scripts/gcloud) ----
# Required for setup-project, setup-database, setup-secrets, build-and-push, deploy
# GCP_PROJECT=
# GCP_REGION=europe-west1
# Optional overrides: SERVICE_ACCOUNT_NAME, CLOUD_SQL_INSTANCE, CLOUD_SQL_EDITION, CLOUD_SQL_TIER,
# DB_NAME, DB_USER, SECRET_NAME_*, ARTIFACT_REGISTRY_REPO, IMAGE_NAME, IMAGE_TAG, SERVICE_NAME.
# If setup-database fails (e.g. ENTERPRISE_PLUS required): CLOUD_SQL_EDITION=ENTERPRISE_PLUS, CLOUD_SQL_TIER=db-perf-optimized-2
# For deploy.sh only (set after build-and-push and setup-database):
# IMAGE=europe-west1-docker.pkg.dev/YOUR_PROJECT/agent-analysis/agent-analysis:latest
# CLOUD_SQL_CONNECTION_NAME=YOUR_PROJECT:europe-west1:trader-edu-db
